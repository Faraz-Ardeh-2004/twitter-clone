# Project Report — Twitter Clone

> This file should be completed per the project spec (Section 6). Under each
> section, the owner and TODO are noted. The real content (not these
> instructions) should replace each section.

## System Architecture
**Owner: Hesam**

<!--
TODO(Hesam):
- Explain the overall architecture (client-server, layering: network ->
  dispatcher -> handler -> DAO)
- Communication flow diagram (client <-> server <-> database) — you can use a
  simple tool (draw.io / excalidraw) and embed the image here.
- Explain the main modules and each one's responsibility (ClientHandler,
  Dispatcher, ConnectionRegistry, the Handlers)
- Architectural pattern used (this project uses a layering similar to MVC +
  Layered Architecture: FXML/Controller = View+Controller on the client,
  Handler = business logic on the server, DAO = data access)
- Thread architecture diagram (per the spec's Phase 4: "draw the server
  architecture diagram - how threads and the dispatcher are distributed")
-->

## Database Design
**Owner: AmirAli**

<!--
TODO(AmirAli):
- Final Entity Relationship Diagram (ERD) — after Phase 3, once all tables are ready
- Explanation of the main entities (users, tweets, follows, likes) and their relationships
- Final DDL code (copied from server/src/main/resources/schema.sql once complete)
-->

## Object-Oriented Design
**Owner: all 3 people (each covering their own part)**

<!--
TODO(everyone):
- Key classes and each one's responsibility (e.g. ClientHandler = one
  connection, DAO = data access, Handler = business logic, Controller = UI bridge)
- Where inheritance/composition/interfaces were used (e.g. ClientHandler implements Runnable)
- Design patterns used:
  - Singleton: DatabaseConnection, NetworkService, UserContext
  - DAO Pattern: UserDAO, TweetDAO, FollowDAO, LikeDAO, SearchDAO
  - MVC-like: FXML (View) + Controller + shared Model
- Optional: UML Class Diagram
-->

## AI Usage Disclosure
**Owner: all 3 people**

<!--
TODO(everyone) — this section is mandatory and sensitive (failure to
disclose can be considered an academic integrity violation):
- Which AI tool(s)/model(s) were used (e.g. Claude, ChatGPT, Copilot)
- Purpose of use (e.g. generating the initial class/project skeleton, help
  debugging, help with documentation)
- Approximate extent of use (e.g. the project skeleton was generated by AI,
  but all business logic/SQL/UI logic was implemented and tested by the team)
- Specific examples of tasks where AI helped
- How the team reviewed/modified/validated the generated content
